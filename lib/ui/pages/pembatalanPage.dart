import 'package:flutter/material.dart';

class pembatalanPage extends StatefulWidget {
  const pembatalanPage({Key? key}) : super(key: key);

  @override
  State<pembatalanPage> createState() => _pembatalanPageState();
}

class _pembatalanPageState extends State<pembatalanPage> {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("pembatalan"),
    );
  }
}
